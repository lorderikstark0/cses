#include <bits/stdc++.h>
using namespace std;

int main(){
  int n ;
  cin >> n;
  vector<int> arr;
  while(n--){
    int a;
    cin >> a;
    arr.push_back(a);
  }
  
  for(size_t i=0;i<arr.size();i++){
    if(arr[i])
  }


  return 0;
}
